# vetr0s.dev

My personal website, written in Org mode and published to plain HTML by Emacs.

```sh
emacs --batch -Q --load ./publish.el --funcall vetr0s-blog-publish
```

GitHub Pages serves the generated `docs/` directory.
