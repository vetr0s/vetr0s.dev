*.zip

# Hugo build output — rebuilt from source by GitHub Actions on every push.
public/
resources/
.hugo_build.lock

# Pulled from vetr0s/resume at build time; that repo is the source of truth.
static/resume.pdf
