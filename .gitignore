*.zip

# Build output, rebuilt from source by GitHub Actions on every push.
public/

# The generator and its toolchain, fetched by CI into the workspace. Never
# committed here: ostat is pinned by commit in .github/workflows/deploy.yml.
.ostat/
.odin/
